load("@pybind11_bazel//:build_defs.bzl", "pybind_extension")

pybind_extension(
    name = "py_camera_model",
    srcs = ["camera_pybind.cc"],
    deps = [
        ":pinhole_camera_model",
        ":brown_camera_model",
        ":omnidirectional_camera_model",
    ],
)
